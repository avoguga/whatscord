node_modules/
dist/
build/
out/
release/
.env
.env.*
!.env.example
.secrets/
*.log
.DS_Store
apps/api/prisma/dev.db
apps/api/uploads/
coverage/
.vite/
*.tsbuildinfo

# Instaladores gerados ficam fora do repositorio.
*.apk
*.aab
*.exe

# Worktrees de agentes. Sao clones locais e temporarios do proprio repo;
# versiona-los criaria um repositorio dentro do repositorio.
.claude/worktrees/

# Catalogos compilados. O plugin do Vite compila o .po no build; versionar o
# .js gerado so cria um segundo lugar para a traducao ficar desatualizada.
apps/web/src/locales/*.js
